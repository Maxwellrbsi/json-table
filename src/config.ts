/** Delay before re-parsing the JSON input while the user types. */
export const DEBOUNCE_MS = 250

/** Containers at this depth or deeper start collapsed on first load. */
export const AUTO_COLLAPSE_DEPTH = 3

/** Rows rendered per array before a "show more" control appears. */
export const ROW_CAP = 200
